name: enviro_example
description: Example Flutter app demonstrating enviro package usage
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  enviro:
    path: ../
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # assets:
    # - .env
    # - .env-development
    # - .env-staging
    # - .env-production
